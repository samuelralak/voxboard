// Test stub for the `server-only` package, which otherwise throws when its module graph is imported
// outside a React Server Component context (e.g. under vitest). The SSR functions under test are pure.
export {};
